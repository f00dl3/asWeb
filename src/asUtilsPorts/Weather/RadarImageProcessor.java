/*
by Anthony Stump
Created: 29 Aug 2017
Updated: 28 Dec 2019
Concept only - not in use.
*/

package asUtilsPorts.Weather;

import java.io.*;

//import magick.

public class RadarImageProcessor {
	public static void brefRules(File inFile) {
/*	
		
		String inFileStr = inFile.getPath();
		
		try {
			ImageInfo info = new ImageInfo(inFileStr);
			MagickImage image = new MagickImage(info);
			DrawInfo aInfo = new DrawInfo(info);
			aInfo.setFill(PixelPacket.queryColorDatabase("yellow"));
			aInfo.setOpacity(100);
			aInfo.setPointsize(36);
			aInfo.setFont("Arial");
			aInfo.setGeometry("+50+50");
			aInfo.setText("jMagick Tutorial");
			image.annotateImage(aInfo);
			image.setFileName(inFileStr);
			image.writeImage(info);
		}
		catch (MagickException mx) { mx.printStackTrace(); }
*/

	}


}